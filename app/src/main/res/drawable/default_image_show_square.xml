<?xml version="1.0" encoding="utf-8"?><!-- 实现椭圆和圆形背景,根据控件的宽高确定(宽=高)是圆、(宽!=高)是椭圆 -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <solid android:color="@color/background_color"></solid>

    <stroke
        android:width="2dp"
        android:color="@color/background_color"></stroke>

</shape><!--
    ** Copyright 2010, Ideasandroid
    **里面我们定义了一个shape对象，实现背景drawable形状，
    其中padding代表距离边框，这里我们设置了左、上、右、
    下四个位置的间距。stroke可以制造出一些3d立体效果，
    corners是四个角，radisu属性可以设置半径，值越大越圆滑，
    根据运行效果你可以微调，最后soild是填充颜色，
    这里我们用了ffffff表示纯白。


    <shape>  android:shape=["rectangle" | "oval" | "line" | "ring"]
    其中rectagle矩形，oval椭圆，line水平直线，ring环形

    <shape>中子节点的常用属性：

    <solid >  实心，就是填充的意思
    android:color指定填充的颜色

    <gradient>  渐变
    android:startColor  起始颜色
    android:endColor  结束颜色
    android:angle  渐变角度，0从上到下，90表示从左到右，数值为45的整数倍默认为0；
    android:type  渐变的样式 liner线性渐变(默认)、 radial环形渐变，径向渐变需要指定半径android:gradientRadius="50"、 sweep 360度扫描渐变

    <stroke > 描边
    android:width 描边的宽度
    android:color 描边的颜色
    android:dashWidth 表示'-'横线的宽度
    android:dashGap 表示'-'横线之间的距离

    <corners > 圆角
    android:radius  圆角的半径 值越大角越圆
    android:topRightRadius  右上圆角半径
    android:bottomLeftRadius 右下圆角角半径
    android:topLeftRadius 左上圆角半径
    android:bottomRightRadius 左下圆角半径

    -->
